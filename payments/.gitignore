.env
.docker/secrets/*
# Binaries for the current project
*.exe
*.dll
*.so
*.dylib
*.o

# Build artifacts
# For Go modules, the default build output is often in the current directory
# or a specific 'bin' directory.
# If you use 'go build -o <output_name>', include that output name.
# Example:
# my_application

# Dependency cache and vendor directories
vendor/
# The go.mod and go.sum files are essential and should NOT be ignored.

# Test cache
# Go's test cache can grow large and is not needed in version control.
.test_cache/

# IDE and editor specific files
.idea/ # IntelliJ IDEA
.vscode/ # VS Code
*.sublime-project
*.sublime-workspace
.DS_Store # macOS specific
Thumbs.db # Windows specific

# Log files and temporary data
*.log
*.tmp
temp/
